#
# Copy and rename this file to 'app.yaml'
#

# Replace YOUR_APP with the TDA registered client id.
# The @AMER.OAUTHAP suffix stays the same
apiKey: YOUR_APP@AMER.OAUTHAP
symbol: SPY
strike: 500
# allowed values are PUT, CALL, ALL
expiration: 2022-01-21
putCall: CALL
# if empty, no slack notifications will be sent
slackWebhookUrl: